# Preview App

This development-only preview app renders the project's React components with mocked data.

## Run

From the repo root:

```bash
npm run preview
```

The preview server runs at http://localhost:5173.
