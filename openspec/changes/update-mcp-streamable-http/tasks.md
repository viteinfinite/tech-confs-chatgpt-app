## 1. Implementation
- [x] 1.1 Update the MCP server to use Streamable HTTP transport with `GET /mcp` and `POST /mcp/messages`.
- [x] 1.2 Add HTTP server configuration for base path `/mcp` and port from `PORT` (default 2091).
- [x] 1.3 Adjust server startup logging for the HTTP listener.
- [x] 1.4 Update README with the HTTP endpoints and connection instructions.

## 2. Validation
- [x] 2.1 Add or update tests to cover the HTTP transport endpoints.
- [x] 2.2 Run `npm test`.
- [x] 2.3 Run `npm run build`.
